---
import Layout from "../layouts/Layout.astro";
import { CheckCircle } from "lucide-react";
---

<Layout title="Transacción Completada | Kuro Custom">
    <div class="min-h-[70vh] flex items-center justify-center py-20 bg-gray-50">
        <div class="text-center animate-fade-in max-w-md mx-auto bg-white p-10 rounded-2xl shadow-sm border border-gray-100">
            <div class="w-24 h-24 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6 shadow-inner">
                <CheckCircle size={48} className="text-green-500" />
            </div>
            <h1 class="text-3xl font-bold text-gray-900 mb-2">
                ¡Pago Exitoso!
            </h1>
            <p id="order-number" class="text-indigo-600 font-bold mb-3 text-lg hidden"></p>
            <p class="text-gray-500 mb-8 leading-relaxed">
                Tu orden ha sido procesada correctamente. Hemos enviado un recibo a tu correo. Estaremos preparando tu envío muy pronto.
            </p>
            <div class="space-y-3">
                <a
                    href="/cuenta"
                    class="block w-full bg-indigo-600 text-white px-8 py-3.5 rounded-xl font-bold hover:bg-indigo-700 transition shadow-md hover:shadow-lg"
                >
                    Ver mis pedidos
                </a>
                <a
                    href="/catalogo"
                    class="block w-full bg-white text-gray-700 border border-gray-200 px-8 py-3.5 rounded-xl font-bold hover:bg-gray-50 transition"
                >
                    Seguir comprando
                </a>
            </div>
        </div>
    </div>
</Layout>

<script>
    const params = new URLSearchParams(window.location.search);
    const orderId = params.get("order");
    if (orderId) {
        const el = document.getElementById("order-number");
        if (el) {
            el.textContent = `#ORD-${String(orderId).padStart(3, "0")}`;
            el.classList.remove("hidden");
        }
    }
</script>
